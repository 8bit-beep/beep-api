spring:
  config:
    activate:
      on-profile: redis
  data:
    redis:
      host: localhost
      port: 6379
      password:
  cache:
    type: redis
  session:
    timeout: 30m
    redis:
      namespace: beep:session
      flush-mode: immediate
