spring:
  config:
    activate:
      on-profile: redis
  data:
    redis:
      host: ${REDIS_HOST}
      port: 6379
      password: ${REDIS_PW}
  cache:
    type: redis
  session:
    timeout: 30m
    redis:
      namespace: beep:session
      flush-mode: immediate
